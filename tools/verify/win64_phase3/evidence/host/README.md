# Host G12 evidence drop

Place Windows host outputs here after `scripts\run_all_host.cmd`:

- `RESULT_HOST.txt` (required)
- optional: individual `*.log` files from package `logs\`

Until `RESULT_HOST.txt` with `OVERALL PASS` exists, Phase 3 G12 is pending.

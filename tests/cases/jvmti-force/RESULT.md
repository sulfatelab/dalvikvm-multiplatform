# JVMTI force-interpreter result

The JNI/JVMTI agent exercises forced-interpreter transitions. Its current
selector is `windows` / `x86_64` / `msvc`.

| Build | Runtime | Last checked |
|---|---|---|
| verified | pending unified managed runner | 2026-07-31 |

The unified catalog owns and rebuilds the native agent here. Historical result
detail remains in the Phase-4 record until the managed source and runner move
into this case; that record does not broaden target applicability.

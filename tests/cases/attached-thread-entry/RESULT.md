# Attached-thread entry result

The `w002attachprobe` JNI DSO exercises native-thread attach and managed-entry
transitions. It is currently applicable only to `windows-x86_64-msvc`.

| Build | Runtime | Last checked |
|---|---|---|
| verified | pending unified managed runner | 2026-07-31 |

The unified Windows cross catalog rebuilt this source from its canonical case
path. Windows AArch64 and ARM64EC remain non-applicable until separately
compiled and run.
